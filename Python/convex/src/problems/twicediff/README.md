# Linear equality constrained problems with twice differentiable convex objective function

min f(x) s.t. Ax=b

f: R^n -> R is convex and twice continuously differentiable

Newton's method can be used for solving such a problem by reducing it to a sequence of linear equality constrained problems.