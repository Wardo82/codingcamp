from .field import Field
from .gf import GFp
from .gfm import Gf

#if __name__ == '__main__':
    ## 9. Fourier interpretation across fields

    # GF(p): Vandermonde
    # GF(p^m): roots of unity in extension
    # GF(2^m): cyclic evaluation via primitive element

    ## DFT matrix:

    # ```python
    #  W[i][j] = ω^(ij)
    # ```

    # works identically.
